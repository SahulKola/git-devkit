import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
    selector: 'app-bytes',
    imports: [RouterLink],
    templateUrl: './bytes.html',
    styleUrl: './bytes.scss',
})
export class Bytes { }
