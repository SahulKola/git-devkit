import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-branching',
  imports: [RouterLink],
  templateUrl: './branching.html',
  styleUrl: './branching.scss',
})
export class Branching {}
